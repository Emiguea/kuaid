<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuaid.dao.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.kuaid.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="is_read" property="isRead"/>
        <result column="extra_data" property="extraData"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, type, title, content, is_read, extra_data, created_at
    </sql>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM `notification`
        WHERE user_id = #{userId}
        ORDER BY created_at DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*) FROM `notification` WHERE user_id = #{userId}
    </select>

    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM `notification` WHERE user_id = #{userId} AND is_read = 0
    </select>

    <insert id="insert" parameterType="com.kuaid.entity.Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `notification` (user_id, type, title, content, extra_data)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{extraData})
    </insert>

    <update id="markAsRead">
        UPDATE `notification` SET is_read = 1 WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllAsRead">
        UPDATE `notification` SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>
</mapper>
