<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuaid.dao.UserMapper">

    <resultMap id="BaseResultMap" type="com.kuaid.entity.User">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="union_id" property="unionId"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="phone" property="phone"/>
        <result column="real_name" property="realName"/>
        <result column="student_id" property="studentId"/>
        <result column="role" property="role"/>
        <result column="balance" property="balance"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, openid, union_id, nickname, avatar_url, phone, real_name, student_id,
        role, balance, status, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM `user` WHERE id = #{id}
    </select>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM `user` WHERE openid = #{openid}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM `user` WHERE phone = #{phone}
    </select>

    <insert id="insert" parameterType="com.kuaid.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (openid, union_id, nickname, avatar_url, phone, real_name, student_id, role, status)
        VALUES (#{openid}, #{unionId}, #{nickname}, #{avatarUrl}, #{phone}, #{realName}, #{studentId}, #{role}, #{status})
    </insert>

    <update id="update" parameterType="com.kuaid.entity.User">
        UPDATE `user`
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="studentId != null">student_id = #{studentId},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateBalance">
        UPDATE `user` SET balance = #{balance} WHERE id = #{id}
    </update>
</mapper>
